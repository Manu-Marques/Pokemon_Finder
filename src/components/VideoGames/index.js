export default function VideoGames(pokemon) {
    return (
        <div>
            {/* {pokemon?.map((poke) => {
                return (
                    <div key={poke.id}>
                        <img src={poke.image} alt="pokemon" />
                        <p>{poke.name}</p>
                    </div>
                )
            })
            } */}
        </div>
    )
}